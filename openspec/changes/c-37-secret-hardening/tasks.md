## 0. Baseline, dependencias y gobernanza

- [x] 0.1 Registrar el baseline de las suites afectadas antes de tocar nada: `cd App/Backend; pytest -m "not integration" -q`, `cd evaluation; pytest -q`, `python -m pytest scripts/preflight -q` y `python -m pytest scripts/dry_run -q`. Verificacion: todas en verde y sin modificaciones; anotar la cantidad de tests. **Baseline: backend 353 passed/22 deselected/1 xfailed; evaluation 69 passed; scripts/preflight 26 passed; scripts/dry_run 41 passed.**
- [x] 0.2 Confirmar los hechos de partida: `git log --oneline -3` incluye `597c03e` y `2784b1b`; `gh api repos/{owner}/{repo} --jq '.security_and_analysis'` reporta `secret_scanning` y `secret_scanning_push_protection` en `enabled`; existe exactamente 1 alerta abierta (numero `1`, `google_api_key`, `resolution: null`). Verificacion: los cuatro hechos quedan registrados en el change. **Confirmado: HEAD `597c03e` (y `2784b1b`); `secret_scanning=enabled`, `secret_scanning_push_protection=enabled`, `non_provider_patterns=disabled`, `validity_checks=disabled`; 1 alerta abierta `#1 google_api_key resolution=null`.**
- [x] 0.3 CHECKPOINT DE GOBERNANZA CRITICAL: antes de ejecutar acciones externas (habilitar en la UI los patrones no-proveedor y las verificaciones de validez; resolver la alerta #1 como `revoked`), presentar el plan exacto y obtener aprobacion humana explicita; registrarla en el change. Verificacion: aprobacion registrada antes de empezar el grupo 5. (Aprobada por el usuario en sesion: "Aplica, una vez termines con esto te explico".) **PENDIENTE — no ejecutado en este apply.**
- [x] 0.4 Confirmar el alcance: no se reescribe el historial de git y no se modifica `.githooks/pre-commit`. Verificacion: `git diff --stat .githooks/pre-commit` vacio y el historial conserva los commits referenciados. **Confirmado: `git diff --stat .githooks/pre-commit` vacio; commits `597c03e`/`2784b1b` presentes.**

## 1. Runbook de endurecimiento (docs)

- [x] 1.1 Crear `docs/security-hardening.md` con las secciones: (a) funciones de escaneo de GitHub configurables por API vs solo-UI; (b) proteccion de push; (c) triaje y resolucion de alertas (`state`/`resolution`); (d) instalacion del hook pre-commit; (e) procedimiento de rotacion de credenciales. Verificacion: el archivo existe y las cinco secciones estan presentes (grep de encabezados). **Creado con 5 secciones `## 1..5` verificadas por grep.**
- [x] 1.2 Documentar el caso de estudio de la filtracion de Gemini: commits `0d430b5`/`eee5c84`, blob `be1ab368`, remocion en `02e845c`, revocacion de la clave y resolucion de la alerta como `revoked`. Verificacion: grep de los hashes y del termino `revoked` en el runbook. **Caso de estudio presente con los cuatro hashes y `revoked`.**
- [x] 1.3 Documentar el procedimiento de rotacion como pasos ordenados: revocar primero en el proveedor, rotar el valor en el `.env` gitignorado, verificar el arbol limpio, y declarar que la remocion del historial de git NO es necesaria una vez revocada la credencial. Verificacion: la seccion enumera los pasos y declara explicitamente que no se reescribe historial. **5 pasos ordenados; declaracion explicita de NO reescribir historial.**

## 2. RED — Tests del script de configuracion

- [x] 2.1 Crear `scripts/security/test_configure_github_secret_scanning.py` con un stub de `gh` inyectado via la variable `GH_BIN` que registra las invocaciones y devuelve JSON configurable. Escribir el test de `--dry-run`: imprime las dos operaciones configurables por API y los pasos solo-UI, sin ejecutar escrituras. Verificacion: el test FALLA (RED) porque el script todavia no existe. **RED confirmado.**
- [x] 2.2 Escribir el test de habilitacion: con estado inicial `disabled`, el script emite `PATCH /repos/{owner}/{repo}` con `secret_scanning` y `push_protection` en `enabled`. Verificacion: RED. **RED confirmado.**
- [x] 2.3 Escribir el test de idempotencia: con estado inicial `enabled` en ambas, el script no emite `PATCH` y reporta que ya estaban habilitadas. Verificacion: RED. **RED confirmado.**
- [x] 2.4 Escribir el test de pasos solo-UI: la salida nombra `secret_scanning_non_provider_patterns` y `secret_scanning_validity_checks` e indica que no son configurables por API. Verificacion: RED. **RED confirmado.**
- [x] 2.5 Escribir el test de entorno incompleto: con `GH_BIN` apuntando a un binario inexistente, el script sale con codigo no-cero y un mensaje accionable, sin reportar exito ni exponer secretos. Verificacion: RED. **RED confirmado.**
- [x] 2.6 Escribir el test de resolucion opt-in: sin `--confirm`, el script no emite `PATCH` de alerta; con `--resolve-alert 1 --resolution revoked --confirm`, emite `PATCH /repos/{owner}/{repo}/secret-scanning/alerts/1` con `state=resolved` y `resolution=revoked`. Verificacion: RED. **RED confirmado.**
- [x] 2.7 Ejecutar `python -m pytest scripts/security/test_configure_github_secret_scanning.py -q` y confirmar RED por ausencia del script; registrar la evidencia. Verificacion: fallo esperado documentado. **`9 failed in 0.21s` por `script not implemented yet`.**

## 3. GREEN — Implementacion del script

- [x] 3.1 Crear `scripts/security/configure_github_secret_scanning.sh` (bash, `set -euo pipefail`) con: resolucion del repo (`gh repo view` por defecto, override `--repo owner/name`), lectura del estado actual, habilitacion idempotente de las dos protecciones API-settable via `gh api`, impresion de los pasos solo-UI, `--dry-run`, override del binario con `GH_BIN`, y resolucion de alerta opt-in con `--resolve-alert N --resolution X --confirm`. Sin secretos embebidos. Verificacion: los tests 2.1-2.6 pasan (GREEN). **GREEN: `9 passed in 3.16s`.**
- [x] 3.2 Hacer el script ejecutable y verificar la sintaxis: `chmod +x scripts/security/configure_github_secret_scanning.sh` y `bash -n scripts/security/configure_github_secret_scanning.sh`. Verificacion: sin errores de sintaxis. **`bash -n` OK; modo `-rwxrwxr-x`.**
- [x] 3.3 Ejecutar `bash scripts/security/configure_github_secret_scanning.sh --dry-run` contra el repo y revisar la salida: imprime las operaciones API-settable y los pasos solo-UI sin modificar GitHub. Verificacion: la salida es correcta y el estado remoto no cambia. **Ejecutado: imprime estado, operaciones API-settable, pasos solo-UI y alerta #1; sin PATCH.**

## 4. TRIANGULATE y REFACTOR

- [x] 4.1 TRIANGULATE: agregar un caso donde `secret_scanning` esta `enabled` pero `push_protection` esta `disabled`; el script debe emitir `PATCH` solo para la proteccion faltante. Verificacion: test nuevo en verde. **`test_partial_state_patches_only_the_missing_protection` en verde.**
- [x] 4.2 TRIANGULATE: agregar un caso con `--repo owner/name` explicito y verificar que el endpoint usa ese repositorio. Verificacion: test nuevo en verde. **`test_explicit_repo_is_used_in_endpoints` en verde.**
- [x] 4.3 REFACTOR: extraer helpers (resolucion de repo, lectura de estado, armado de payload y de la lista de pasos manuales) sin cambiar comportamiento. Verificacion: la suite del script sigue en verde tras cada refactor y `shellcheck scripts/security/configure_github_secret_scanning.sh` no reporta errores (si `shellcheck` esta disponible). **Helpers `resolve_repo`/`read_status`/`enable_protections`/`ui_steps`/`list_alerts`/`resolve_alert`/`gh_api`; refactor de la lista solo-UI a una fuente unica. Suite `9 passed` tras el refactor. `shellcheck` no esta instalado (omitido).**
- [x] 4.4 Confirmar que el script no contiene valores de credenciales ni comandos de reescritura de historial. Verificacion: grep negativo de `filter-branch`/`filter-repo` y de patrones de clave. **Grep negativo para `filter-branch`/`filter-repo` y para patrones de clave Google/PEM.**

## 5. CRITICAL — Configuracion de GitHub y resolucion de la alerta (con checkpoint del 0.3)

- [x] 5.1 Con la aprobacion del 0.3 registrada, habilitar en la UI de GitHub los patrones no-proveedor y las verificaciones de validez. Verificacion: el estado reportado por `gh api repos/{owner}/{repo} --jq '.security_and_analysis'` (o el registro del operador) confirma ambos en `enabled`. RESUELTO COMO NO DISPONIBLE (apply 2026-09-18): no es completable por API (se reintento `PATCH /repos/{owner}/{repo}` con y sin header `X-GitHub-Api-Version: 2026-03-10`; GitHub ignora ambos campos y los reporta `disabled`) ni por UI (el operador confirmo que los toggles no existen en `Settings > Security and quality > Advanced Security`). Requieren GitHub Secret Protection (GitHub Team/Enterprise, plan pago). Decidido en design.md D9: se cierran como no disponibles; el script y el runbook lo reportan explicitamente.
- [x] 5.2 Ejecutar `bash scripts/security/configure_github_secret_scanning.sh` sin `--dry-run` para habilitar por API las protecciones y confirmar el estado. Verificacion: `gh api repos/{owner}/{repo} --jq '.security_and_analysis'` reporta `secret_scanning=enabled` y `secret_scanning_push_protection=enabled`. (Ejecutado: idempotente, ambas ya `enabled`, sin PATCH.)
- [x] 5.3 Resolver la alerta #1 como `revoked` con confirmacion explicita. Verificacion: `gh api repos/{owner}/{repo}/secret-scanning/alerts/1 --jq '{state,resolution}'` reporta `state=resolved` y `resolution=revoked`. (Ejecutado: `resolved`/`revoked`.)
- [x] 5.4 Verificar que no quedan alertas abiertas. Verificacion: `gh api "repos/{owner}/{repo}/secret-scanning/alerts?state=open" --jq 'length'` devuelve `0`. (Ejecutado: `0`.)

## 6. Verificacion final

- [x] 6.1 Ejecutar `python -m pytest scripts/security/test_configure_github_secret_scanning.py -q` y confirmar la suite del script en verde. Verificacion: todos los tests pasan. **`9 passed`.**
- [x] 6.2 Ejecutar las suites existentes afectadas (`App/Backend` SQLite, `evaluation`, `scripts/preflight`, `scripts/dry_run`) y confirmar que no hay regresiones respecto del baseline del 0.1. Verificacion: mismas cantidades o mayores, sin fallos. **Backend `353 passed/22 deselected/1 xfailed`; evaluation `69 passed`; preflight `26 passed`; dry_run `41 passed` — identicos al baseline.**
- [x] 6.3 Verificar la parametrizacion de credenciales locales: grep de `${POSTGRES_PASSWORD:-` y `${N8N_BASIC_AUTH_PASSWORD:-` en `docker-compose.yml` y `App/Backend/docker-compose.yml`; confirmar defaults `mesa_local_dev`/`n8n_local_dev` y ausencia de `mesa`/`admin` como password por defecto. Verificacion: los greps coinciden con lo esperado. **`POSTGRES_PASSWORD:-mesa_local_dev` y `N8N_BASIC_AUTH_PASSWORD:-n8n_local_dev`; ningun default exactamente `mesa`/`admin`.**
- [x] 6.4 Verificar que `.githooks/pre-commit` no fue modificado y que el historial de git no fue reescrito. Verificacion: `git diff --stat .githooks/pre-commit` vacio y los commits `0d430b5`/`eee5c84`/`02e845c` siguen presentes. **Diff vacio; los tres commits presentes (`git cat-file -t` = commit).**
- [x] 6.5 Ejecutar `openspec validate c-37-secret-hardening --strict` y confirmar PASS. Verificacion: salida valida. **`Change 'c-37-secret-hardening' is valid`.**
- [x] 6.6 Confirmar con `git status --porcelain` que no quedan secretos, credenciales ni artefactos temporales en el cambio. Verificacion: arbol limpio salvo los archivos previstos por el change. **Solo los archivos previstos (`docs/security-hardening.md`, `scripts/security/*.sh|*.py`, `openspec/changes/c-37-secret-hardening/**`); `__pycache__` gitignorado.**
