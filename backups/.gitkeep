# This directory holds PostgreSQL backup files (*.sql).
# Backups are generated by scripts/backup.sh (Linux/macOS) or scripts/backup.ps1 (Windows).
# The scripts keep the last 7 daily backups and rotate older ones automatically.
# Backup files are NOT tracked in git — see .gitignore.
